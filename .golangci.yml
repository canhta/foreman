version: "2"
linters:
  enable:
    - bodyclose
    - misspell
    - noctx
    - unconvert
  settings:
    errcheck:
      check-type-assertions: true
    govet:
      enable-all: true
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - errcheck
        path: _test\.go
      - linters:
          - govet
        text: unusedwrite|fieldalignment
        path: _test\.go
    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
